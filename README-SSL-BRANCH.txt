This branch adds ssl support.
Please copy the file server.keystore.sample to $HOME/server.keystore
Each system should have its own keystore so it is not tracked by git
and the keystore password is configured by the system property
ssl.keystore.password
